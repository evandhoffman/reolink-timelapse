# ── NVR connection ─────────────────────────────────────────────────────────
NVR_HOST=192.168.1.100
NVR_USERNAME=admin
NVR_PASSWORD=your_password_here

# ── Capture settings ───────────────────────────────────────────────────────
# Seconds between snapshots (float OK, e.g. 15.0)
CAPTURE_INTERVAL_SECONDS=15

# How many hours to run before stopping automatically
DURATION_HOURS=18

# Comma-separated list of channel numbers to capture (empty = all online cameras)
# Run 'test' mode to see channel numbers.  Example: 0,2,3
CAPTURE_CHANNELS=

# ── Stitch settings ────────────────────────────────────────────────────────
# Use every Nth captured frame when stitching (1 = use all frames)
# Example: captured at 1/15s → STITCH_EVERY_N_FRAMES=4 → effective 1 frame/minute
STITCH_EVERY_N_FRAMES=1

# Output video frame rate
OUTPUT_FPS=24

# ── Storage ────────────────────────────────────────────────────────────────
# Must match the container path in the docker-compose volume mount
DATA_DIR=/data
